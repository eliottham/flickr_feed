<div class='flickr'>
	<ul>
	<% photos.each do |row| %>
		<li>
			<% row.each do |p| %>
				<%= link_to image_tag(p[:image], class: 'flickr_photo'), p[:link] %>
			<% end %>
		</li>
	<% end %>
	</ul>
</div>